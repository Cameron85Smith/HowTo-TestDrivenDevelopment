namespace CalculatorTest
{
    // 2) Write dummy logic to fail the test (Red Phase).
    // Then write the intended production logic to turn the test green (Green Phase).
    // 3) Refactor the logic.
    public class Calculator
    {
        public int AddNumbers(int a, int b) => a + b;

        public int SubtractNumbers(int a, int b) => a - b;
    }
}